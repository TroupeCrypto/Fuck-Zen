/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "standalone",
  experimental: {
    // keep defaults; this file exists to make config explicit & stable
  }
};

export default nextConfig;
